import { TrendingUp, TrendingDown } from 'lucide-react'; const gradients = { violet: 'bg-indigo-700', orange: 'bg-orange-600', emerald: 'bg-emerald-600', rose: 'bg-rose-600', blue: 'bg-blue-600', amber: 'bg-amber-600', slate: 'bg-slate-600',
}; const KpiCard = ({ label, value, delta, icon: Icon, accent = 'slate' }) => { const hasDelta = delta != null && Number.isFinite(delta); const positive = hasDelta && delta >= 0; const gradient = gradients[accent] || gradients.slate; return ( <div className="bg-white rounded-lg border border-slate-100 p-5 flex items-center gap-4"> {Icon && ( <div className={`h-14 w-14 rounded-md flex items-center justify-center shrink-0 ${gradient}`}> <Icon size={26} className="text-white" strokeWidth={2} /> </div> )} <div className="min-w-0 flex-1"> <p className="text-sm text-slate-500 truncate">{label}</p> <div className="flex items-baseline gap-2 mt-0.5"> <h3 className="text-2xl font-bold text-slate-900 tracking-tight truncate">{value}</h3> {hasDelta && ( <span className={`text-xs font-semibold shrink-0 inline-flex items-center gap-0.5 ${positive ? 'text-emerald-600' : 'text-rose-600'}`}> {positive ? <TrendingUp size={12} /> : <TrendingDown size={12} />} {Math.abs(delta).toFixed(1)}% </span> )} </div> </div> </div> );
}; export default KpiCard;
